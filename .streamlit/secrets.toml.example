# For Streamlit Cloud: paste this block into the app's "Secrets" dashboard.
# For local dev: copy to `.streamlit/secrets.toml` (gitignored) OR use `.env`.

DB_HOST = ""
DB_PORT = "5432"
DB_NAME = "postgres"
DB_USER = ""
DB_PASSWORD = ""

APP_PASSWORD = ""
